<form method="post" action="{{ path('app_kind_delete', {'id': kind.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kind.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
