<form method="post" action="{{ path('app_kinds_delete', {'id': kind.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kind.id) }}">
    <button class="btn btn-danger mt-2">Supprimer</button>
</form>
