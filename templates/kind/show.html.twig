{% extends 'base.html.twig' %}

{% block title %}Kind{% endblock %}

{% block body %}
    <div class="container">
        <h1>Genre</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kind.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ kind.name }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('app_kinds_index') }}" class="btn btn-info mt-2">back to list</a>

        <a href="{{ path('app_kinds_edit', {'id': kind.id}) }}" class="btn btn-primary mt-2 mx-2">edit</a>

        {{ include('kind/_delete_form.html.twig') }}
    </div>
{% endblock %}
